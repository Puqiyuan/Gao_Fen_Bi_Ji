已知一个带有表头结点的单链表, 结点结构为:
                             ---------
			    |data|next|
			     ---------
假设该链表只给出了头指针head. 在不改变链表的前提下, 请设计一个尽可能高效的算法,查找链表中
倒数第k(k为正整数)个位置上的结点. 若查找成功, 算法输出该结点的data值, 并返回1;否则, 只返回
0.
