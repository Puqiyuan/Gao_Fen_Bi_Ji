已知一个整数序列A = (a0, a1, ..., a(n - 1)), 其中0 <= a(i) < n (0 <= i < n). 若存在a(p1) =
a(p2)= ... = a(pm)= x, 且m > n / 2(0 <= pk < n, 1 <= k <= m), 则称x为A的主元素. 例如, A =
(0, 5, 5, 3, 5, 7, 5, 5), 则5为主元素; 又如A = (0, 5, 5, 3, 5, 1, 5, 7), 则A中没有主元素.
假设A中的n个元素保存在一个一维数组中, 请设计一个尽可能高效的算法, 找出A的主元素. 若存在主
元素, 则输出该元素; 否则输出-1.
